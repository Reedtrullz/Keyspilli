# Approved execution checklist

Objective: execute `2026-09-07-autonomous-metal-piano-delivery.md`; approval supplied in attachment `d4b0e1e8-d6f0-4abc-b65b-1e5195f2a969/pasted-text-1.txt`.

Final no-release audit is in `metal-final-report-2026-09-07.md`; frozen results in `metal-beta-acceptance-2026-09-07.json`. Historical checkpoints below retain their original status and are superseded by this checklist.

- [x] Task1: bounded reproducible development runner — COMPLETE.12 tests pass (runner3/evaluator9), catalog typecheck passes; all12 development cached stems replayed,11 structural pass and Silent Lucidity remains failed; second run12 cache hits. No inference or holdout invocation.
- [x] Task2: COMPLETE via explicit no-promotion branch: six development targets inspected, zero fully qualified real reference pairs; three independent native piano controls rendered/scored. Exact Basic Pitch precision 0.690–0.845 fails the fixed0.95 gate. References remain outside generation inputs.
- [x] Task3: COMPLETE via no-promotion branch. Two algorithms, two configurations each: piano specialist improves controls but fails Debussy precision; pyin passes invented upper line but fails bass timing. Real separated vocal/bass confidence diagnostic and12 cached-role previews retained.18 service tests and typecheck pass. No production adapter enabled.
- [x] Task4: COMPLETE. Paired identity and physical-key overlap regressions pass; all-level synthetic lineage checked.363 MIDI/55 catalog tests and three typechecks pass. Final12 cached replays all structural pass (Silent Lucidity9→7). Third/final selector iteration retained; no audio promotion.
- [x] Task5: indexed native implementation verified; real full-song release exit FAILED (zero eligible sources), carried to authorized no-release branch.
- [x] Task6: owned lease, duplicate/cancellation/retry preservation,14 failure categories, source UI and Cathedral/player checks complete.
- [x] Task7:5/5 actual API round trips, exact code-head CI42/42 browser, frozen12-song/72-preview bundle. Musical release gates remain failed.
- [x] Task8: PR82 no-release candidate; no canary/merge/deploy because zero routes qualify. Final documentation-head CI checked on PR before handoff.
- [x] Final deliverables: report and acceptance JSON enumerate every task, failed gates, preservation interval and runnable artifacts. Strong musical certification pending.

Starting state verified: clean `codex/metal-delivery` at `e4e4f04`; Node22.22.3;56GiB free. Original workspace has unrelated catalog/report WIP and remains untouched. No active inference/build found; existing listening HTTP server PID25107 is live. Deployment identity read requested; output retained privately. Holdout sources remain unrun.

Task1 commands/logs: `output/metal-development/runner-red.log`, `runner-final.log`, `runner-typecheck.log`, `runner-replay.log`, `runner-cache.log`; runner invocation accepts `--manifest`, `--hashes`, `--recordings`, `--cached-stems`, `--output`, optional `--ids`. Fresh inference additionally requires `--model-manifest`, with optional explicit Python/BasicPitch executables. Production image still `c36680100de9`, digest `sha256:867c6f9908926a49576d4cc1bed2150e67897e8a848a7fa7cca63a7cf3943ab2`.

Task4 checkpoint: new paired identity regression reproduces contaminated-theme survival15/16 (private `identity-red.log`). First exploratory change admitted all raw guitar notes>=36 before rhythm routing: low case improved, but opposite high lead dropped to8/16, isolated-low/dyad behaviors regressed, and two diagnostic/rescue checks changed (120 pass/5 fail). Rejected and restored production source before proceeding; test remains intentionally red/uncommitted for root-cause work. This consumes one of three meaningful selector iterations. No candidate retained or deployed. Next: evaluate coherent low phrase before co-onset rhythm exclusion while preserving repeated-wall and dyad rejection; inspect later identity mutation and serialization overlap. Tasks4–8 still incomplete.

Task3 private runnable experiments: `piano-challenger/run.py` (verified model path), `postprocess.py` (second configuration, reuses activations), `pyin-trial.py`, `pyin-trial-long-window.py`, `mono-real/track.py`, `role-previews/render.mts`. Exact input/model/code hashes are recorded in comparison report; references remain outside generation. Initial model trial CPU13–14seconds per30second control; no general runtime promise. Logs `upstream-tests.log`18 pass and `upstream-typecheck.log`exit0. Obsidian project section Autonomous execution checkpoint and daily Log updated.

Task4 final evidence: `metal-identity-preservation-2026-09-07.json`. Rejected iteration1 is restored; iteration2 phrase support plus iteration3 shared cleaned harmony/rhythm evidence retained. Twelve final reports at `output/metal-development/replay-task4-final`; exact test logs `task4-midi-tests.log`, `task4-catalog-tests.log`, `task4-typecheck.log`. No further selector tuning is authorized under the three-iteration ceiling.

Task5 checkpoint: moved existing Brave provider to catalog (`source-candidate-provider.ts`), web wrapper reexports it; commit b12d76f. Existing10 provider tests and catalog/web typechecks pass. Worker integration/acquisition is not yet implemented, so discovery is not claimed automatic. Reuse `researchExternalCandidates` with generation inputs, `freezeGenerationCandidateSet`, and `buildExternalSymbolicArrangement`; native intake already bounds16MiB and shared retrieval handles manual redirects/literal private-IP rejection. Before enabling network, ensure DNS-resolved private targets and bounded total timeout are covered. Native adapter stores tempoMap but downstream native-time preservation must be proved (do not equate first tempo with full timing). Existing frozen-route adapter rejects file-like sourceRef and remote acquisition; use logical IDs plus explicit actual-source URL lineage without pretending remote media was user uploaded. Provider's Brave configuration/free quota has not been verified; unknown quota means skip calls and support only explicitly eligible indexed sources, with truthful limited-scope review outcomes. No source route qualifies yet.

Task5 implementation checkpoint: `automatic-symbolic.ts` resolves requested recording IDs against an operator-verified, hash-pinned native MIDI index; bounds acquisition, rejects ambiguous/unverified sources and protected original hashes, normalizes source tempo changes through native seconds, and reuses research ingestion→freeze→direct-piano builder. New worker path is disabled unless `KEYSPILLI_SOURCE_ASSISTED_BETA=1` and an explicit index path exists. No index is shipped or enabled yet. Actual source/timing/melody/license provenance is validated and persisted to manifest/all public levels. Current checks:46 service tests,72 focused catalog tests and catalog/transcribe/web typechecks pass. Public Mutopia network acquisition via new checked-DNS/pinned-address transport returned2643-byte valid MIDI; this is transport proof, not a qualifying full-song case. Existing source resolver32 tests pass including new DNS-private-target regression.

Still required for Task5: actual isolated worker end-to-end native run, reference firewall configuration at the worker boundary, source index/corpus eligibility and meaningful source resolution coverage, final source-route evidence. Prior Livgardet/ABBA extractors inspected: explicit hardcoded key centers/resolutions/scanlines/delay; no general geometry validation or audio-onset agreement. Tutorial route remains unimplemented/ineligible pending bounded support decision; piano-audio is excluded by Task3. Automatic web search free quota is unverified, so no paid/provider calls were made. Task6 must replace temporary status-only native publication checks with owned job leases, prove idempotency/cancellation, and expose beta/source labels. No production mutation or holdout use.

Task5 reference-boundary follow-up: worker resolver now always loads a metadata-only protected hash registry containing the nine frozen control source/MIDI/audio hashes. Caller omission or an empty caller list cannot disable protection. Pinned original hashes are rejected before acquisition and the merged firewall reaches research/freeze/build. Evidence-shaped index records are rejected instead of silently reclassified. Regression reproduced failure before fix;5 resolver tests and transcribe typecheck pass. No reference note payload is loaded by generation. Actual full-song worker evidence and remaining Tasks5–8 are still outstanding.

Task5 bounded source decision: see metal-source-route-decision-2026-09-07.json. Actual processJob now importable without starting poll loop; production CLI entry still starts loop. Isolated queued synthetic recording resolves through network-substituted retrieval, real parser/builder/database/publication to all five public levels.48 service tests and typecheck pass. This is plumbing proof only. Six development composition leads are paid or lack verified production rights; no eligible index shipped. Tutorial scripts lack the required general calibration evidence, so unsupported; piano-audio excluded by Task3. Task5 release exit remains failed (no qualifying real full-song automatic source), explicitly carried into no-release evidence rather than pretending synthetic success qualifies. Continue Task6 safety and complete Tasks7–8 no-release branch unless new eligible evidence appears without exceeding limits.

Task6 lease/fallback checkpoint: DB migration adds owner token and integer expiry; claim returns a fresh token; renewal/finalization require live matching ownership. Worker heartbeat and all status writes use the token, and polling reclaims expired jobs. Fake-clock20-minute healthy/crash/reclaim/stale-finalization regression passes. Real processJob test changes owner during acquisition and confirms no publication/finalization by old worker. Shared publication now invokes cancellation check after complete staging/validation before swap; rejection preserves prior tree. Separator failure no longer removes diagnostics or falls through to unqualified full-mix publication; real worker failure regression proves error with no song.43 catalog tests (jobs/ingest/publish),50 service tests and catalog/transcribe/web typechecks pass. Logs task6-lease-catalog.log, task6-lease-worker.log, task6-lease-typecheck.log; red logs retained. Remaining: duplicate/source-version validation, full failure matrix, beta UI/player, exact CI and final evidence/PR. No live DB migration or deployment performed.

Task6 duplicate/source-label checkpoint: existing native rows are reusable only after full source/realization hash match, all five public rows, and existing MIDI/MusicXML/notes semantic validation. Duplicate fixture preserves manifest bytes; corrupted prior manifest becomes review without overwrite (red regression reproduced old false-done behavior). Player full-detail and direct-sheet metadata expose validated source provenance; both render beta, melody/accompaniment status, selected source, requested recording and source-owned timing.20 focused web tests and web/transcribe typechecks pass; worker regression suite3 passes. UI rendering is currently static-component/API evidence; browser/audio verification remains pending.

Task6/7 integration checkpoint f26a068: merged current origin/main c366801, including Cathedral implementation absent from prior worktree. Added real PlaybackEngine→Cathedral LH/RH gain/scheduling check;120 player tests pass. Source-labelled full/sheet APIs and safe review messages covered by23 focused web tests. Worker matrix now60 service tests covering14 required failure categories, plus source-version and prior-artifact hashes. Report metal-worker-safety-2026-09-07.json states synthetic/fault-injection limits. Twelve merged cached development replays all structural pass; no model inference or holdouts.

Task7 exact CI sequence is running at f26a068 in disposable detached checkout /Users/reidar/Projectos/.keyspilli-worktrees/metal-ci because fetch-seed hardcodes repository paths. Live exec session73932 owns output/metal-development/run-ci.py; do not restart on observation timeout. Results/logs output/metal-development/ci-f26a068. First12 commands passed through build, Chromium runtime and edge boundary; Playwright is in progress. Fetch-seed reports108 fetched/6 preserved,2 download failures, exit0; pipeline110 successful/0failed,660rows. No task completion or release claim yet. Need final browser/API export verification, listening/review bundle, frozen acceptance and PR exact-head CI.

Task7 CI correction: first f26a068 sequence finished: all12 pre-browser commands exit0, Playwright exit1 (30pass/3fail/9notrun). Traces show pending upload/detail requests and slow initial PDF export under development server; did not relabel pass. Commit429c697 makes CI use already-built production server, disables server reuse in CI and bounds two workers without weakening timeouts. Full exact-command rerun now owned by live session87953 via run-ci.py with CI=true, logs ci-429c697. Previous73932 is completed, not running. Disk35GiB before retry, per-stage30GiB guard. Review bundle and exact API export comparison still pending.
