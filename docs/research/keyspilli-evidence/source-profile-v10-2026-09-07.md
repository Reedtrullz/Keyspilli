# Source-profile v10 local application proof

The user accepted the revised Nothing Else Matters Easy passage. Applied the current reducer to six existing tutorial candidates (Queen, two Metallica arrangements, In My Mind, Nirvana, AC/DC).

## Verified

- All six complete variant sets pass structural validation.
- An isolated actual ingest with YouTube content type, source profile, no transcription cleanup and no duration ceiling persists all24 public MIDI note sets exactly as generated by the reducer.
- SQLite integrity check returns ok.
- Local Next.js on port3102 serves six Easy player pages successfully.
- All24 public MIDI export requests return HTTP200 and bytes identical to persisted artifacts.
- Nirvana and AC/DC listening-page exports now come from that isolated catalog.
- The user's accepted Metallica Easy clip remains unchanged.

## Reproduction

Private media remains outside git. From repository root with Node22:

- output/tutorial-recovery/verify-ingest-v10.mts (requires a fresh isolated data directory)
- output/tutorial-recovery/verify-http-v10.mts (local Next.js must use that isolated directory)
- output/tutorial-recovery/refresh-ingested-review.mts (renders the persisted MIDI)

Committed JSON files contain the per-song note counts and per-export SHA256 receipts.

## Remaining boundaries

This is local ingest and HTTP export proof, not automatic production-worker integration, browser audio verification, broad musical acceptance, or source-rights clearance. The worker's verified-native route still cannot treat a tutorial-derived MIDI as verified native MIDI. Existing production songs are untouched.
