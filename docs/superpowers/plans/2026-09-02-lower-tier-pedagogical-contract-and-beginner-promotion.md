# Lower-tier Pedagogical Contract and Beginner Promotion Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to execute this plan task-by-task.

**Goal:** Decide whether the already-validated collision-aware sparse-LH Beginner policy generalizes across safe repository fixtures and, only if every gate passes, promote it as the documented Beginner contract.

**Architecture:** Freeze current lower-tier behavior and eligible symbolic fixtures, then run one scratch evaluator using the exact `COLLISION_AWARE_SPARSE_LH` rule. If the expanded evidence selects the validation outcome, make the smallest production change by reusing the existing sparse-anchor primitive and update truthful metadata/docs; otherwise commit only the evidence report and leave production unchanged.

**Tech Stack:** TypeScript/Node, `@keyspilli/midi`, Vitest, workspace npm scripts, deterministic JSON, Git, Obsidian project notes.

**Spec:** `/Users/reidar/.codex/attachments/b7bfde39-b745-49c3-85e8-e14522ede060/pasted-text.txt`

## Global Constraints

- Candidate is exactly `COLLISION_AWARE_SPARSE_LH`; do not tune or design another LH algorithm.
- Candidate Beginner RH must remain byte-identical; never retime, replace, or remove RH notes.
- Candidate sounding `maxSimultaneity` must remain `<= 2`; existing Beginner density, IOI, grid, duration, span, and timing gates must pass.
- Use only existing `GENERATION_DEV_ELIGIBLE` symbolic fixtures; exclude `EVAL_ONLY`, commercial/protected/reference MIDI, and new downloads/research.
- No human audit, audio render, production replay, upstream transcription research, or reference upload.
- Do not alter Very Beginner, Very Easy, Easy, Medium, or Advanced.
- No Case-05 special case; genuine rests must remain silent.
- Structural LH events require existing trusted structural/root/bass evidence; unknown unsafe material fails closed.
- If outcome is not `BEGINNER_SPARSE_TWO_HAND_CONTRACT_VALIDATED`, production remains unchanged.
- Preserve deferred `COVER_RH_IDENTITY_CLIFF` and `DIFFICULTY_DIFFERENTIATION_CONCERN`.
- Keep all reports path-safe and deterministic; do not stage `.tmp-source-audit/` or `pnpm-lock.yaml`.

## Tasks

### Task 1: Freeze contracts, fixtures, and preregistration

Inspect actual code/config for Very Beginner, Beginner, and Very Easy, classify current alignment, inventory every safe symbolic fixture, and write/hash `beginner-sparse-lh-production-preregistration.json` before aggregate evaluation. No production source changes.

### Task 2: Expand the isolated evaluator

Reuse the prior scratch evaluator and exact sparse-anchor semantics. Add corpus eligibility, per-level freeze/digests, RH parity, structural LH provenance, any-hand active-window erasure, physical two-hand metrics, collision/rest regressions, and deterministic JSON output. Keep evaluator and test inputs outside production paths unless a pure test helper is strictly required.

### Task 3: Evaluate and select one outcome

Run the preregistered evaluator once over all eligible fixtures plus synthetic rest/collision regressions and compare Very Beginner/Candidate Beginner/Very Easy. Classify each fixture `IMPROVED`, `UNCHANGED`, or `REGRESSED`; select exactly one of the four specified outcomes and write a path-safe results report.

### Task 4: Conditional production promotion

Only when Task 3 selects `BEGINNER_SPARSE_TWO_HAND_CONTRACT_VALIDATED`, reuse/extract the smallest existing sparse-anchor primitive in the production Beginner path, update Beginner metadata/docs to `PRIMARY_MELODY_PLUS_SPARSE_STRUCTURAL_LH`, add focused regressions, and prove all non-Beginner outputs are unchanged. Otherwise make no production change.

### Task 5: Final verification and closeout

Run focused MIDI tests, full workspace tests, all workspace typechecks, `git diff --check`, deterministic evaluator rerun, six-level/non-Beginner parity, remote SHA parity, and disk check. Commit/push only intended docs and conditional source changes, log the decision in the Keyspilli Obsidian project note, and retain deferred concerns.
